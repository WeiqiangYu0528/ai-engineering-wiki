---
title: "Reading path: AI Agents & Tool Use"
---

# Reading path: AI Agents & Tool Use

> 9 pages in order, about 39 minutes of reading · content as of 2026-08-27

Each entry assumes the ones above it, which is the only claim this page makes — it is a route in, not a table of contents. The [AI Agents & Tool Use map](/agents/) has the full list.

1. [AI Agents](/agents/concepts/ai-agents) — the definition the rest of the domain argues with: an LLM using tools in a loop
2. [Agent Components](/agents/concepts/agent-components) — planning, tools and memory; the anatomy the remaining entries fill in
3. [Tool Use](/agents/concepts/tool-use) — the leg that makes the loop act on anything outside its own context
4. [Function Calling](/agents/concepts/function-calling) — the structured-emission mechanism underneath step 3
5. [Model Context Protocol](/agents/concepts/model-context-protocol) — what step 4 becomes once the tool interface is standardized across hosts
6. [AI Workflows](/agents/concepts/ai-workflows) — the predefined-code-path alternative, which assumes the augmented LLM of steps 3–5 and is the rung before autonomy
7. [Context Engineering](/agents/concepts/context-engineering) — the binding constraint once a loop runs for many turns rather than one
8. [Deep Agents](/agents/concepts/deep-agents) — the long-horizon architecture that exists because of step 7
9. [Context Engineering: One Constraint Seen From Three Layers](/agents/concepts/context-engineering-thesis) — the synthesis: those context decisions are inference-cost decisions

Every page here names the one before it in its header and links the one after it at the foot of its body, so the order can be followed without coming back to this page.
